# Secrets
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

#kubectl get secret argocd-tls-cert -n argocd -o jsonpath='{.data.tls\.crt}' | base64 -d > argocd.crt


resources:
  - kubernetes/namespace.k8s.yaml
  - kubernetes/secret.k8s.yaml
  - kubernetes/ingress.k8s.yaml
  # - patches/resources/certificate.crd.yaml
  # - patches/resources/issuer.crd.yaml
  
  
  
  - patches
