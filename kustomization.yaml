# Secrets
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - kubernetes/namespace.k8s.yaml
  - kubernetes/secret.k8s.yaml
  - kubernetes/ingress.k8s.yaml
  
  
  
  
  - patches
