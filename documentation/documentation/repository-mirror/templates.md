# templates

Purpose: Index of templates kept in `obsidian/templates/` used for documentation, runbooks, and repeatable artifacts.

What you'll find:
- skeleton files and example notes that help maintain consistent documentation style.

When to use:
- Copy templates into a new folder under `obsidian/documentation/repository-mirror/` or directly into `obsidian/` when creating new documentation entries.
