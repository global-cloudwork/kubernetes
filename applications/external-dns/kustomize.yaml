apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/managed-by: argo-cd

helmCharts:
  - name: external-dns
    repo: https://charts.bitnami.com/bitnami
    releaseName: my-external-dns
    namespace: argocd
    version: 9.0.0

configMapGenerator:
  - name: external-dns
    files:
      - external-dns.properties