apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: base-manifests
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: 'https://github.com/global-cloudwork/kubernetes'
        revision: 'main' 
        files:
          - path: 'kubernetes/base/*/*.yaml'
  template:
    metadata:
      name: '{{path.basename}}--{{timestamp}}' 
    spec:
      project: default
      source:
        repoURL: 'https://github.com/global-cloudwork/kubernetes'
        targetRevision: 'main'
        path: '{{path}}'
      destination:
        server: 'https://kubernetes.default.svc'
        namespace: argocd  
      syncPolicy:
        automated:
          prune: true
          selfHeal: true