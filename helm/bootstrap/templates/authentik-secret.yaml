apiVersion: v1
kind: Secret
metadata:
  name: authentik-secret-key
  namespace: {{ .Values.namespace.authentik.name }}
  labels:
    app.kubernetes.io/name: bootstrap
type: Opaque
stringData:
  secret_key: {{ .Values.authentik.secretKey | quote }}
