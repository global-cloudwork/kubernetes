apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
  namespace: {{ .Values.namespace.argocd.name }}
  labels:
    app.kubernetes.io/name: bootstrap
data:
  hydrator.enabled: {{ .Values.argocd.hydrator.enabled | quote }}
  commit.server: {{ .Values.argocd.hydrator.commitServer | quote }}
