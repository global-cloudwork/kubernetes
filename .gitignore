# Credential and environment files
*.key
**/*key.json
*.env
.env

# Kubernetes secret manifests contain credentials. Example files use the
# .yaml.example suffix and are not matched by these rules.
**/*secret*.yaml
**/*secret*.yml

key.*
kilo.jsonc
.kilo/*

# Keep generated chart caches out of the repository.
applications/**/charts/
